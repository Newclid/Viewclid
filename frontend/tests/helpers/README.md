# Test helpers

Store reusable builders, fakes and test-specific utilities here.

Helpers should make tests easier to read without hiding the behaviour being
verified. Do not place production logic in this directory.
