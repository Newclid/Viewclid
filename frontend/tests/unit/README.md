# Unit tests

Place tests here when one frontend subsystem is tested in isolation.

Examples:

- Geometry and viewport calculations
- Scene state and undo behaviour
- JGEX parsing and generation
- Application store transitions
- Backend client request construction with `fetch` mocked
- Individual UI components with their stores and callbacks mocked

Unit tests must not start real external services.
