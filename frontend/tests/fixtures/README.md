# Test fixtures

Store reusable, immutable test data here, for example:

- Scene snapshots
- Backend job responses
- Proof results
- Custom theorem definitions

Prefer small builder functions when a test needs to vary only a few fields.
